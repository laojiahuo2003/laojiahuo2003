# 🏠 主页架构说明（v4）

## 页面结构

| 区块 | 来源 | 刷新频率 |
|---|---|---|
| Banner | `assets/banner.svg`（`scripts/gen_banner.py` 生成） | 手动 |
| 足迹条 | `assets/journey.svg`（`scripts/gen_live_cards.py`） | 每小时 |
| Daily Picks | `assets/picks.svg`（同上，数据来自 github-daily-report） | 每小时 |
| Projects | README 文字 | 手动 |
| Stats | `assets/stats.svg`（同上） | 每小时 |
| 贪吃蛇 | `output/snake*.svg/gif`（`snake.yml`） | 每天两次 + push |

## 工作流

- `live-cards.yml` — 每小时 :23 拉取 GitHub API + 日报仓库，重新生成三张卡片
- `snake.yml` — 每天两次生成贡献贪吃蛇

## 改内容

- **Banner 文案**：改 `scripts/gen_banner.py` 里的常量 → `python3 scripts/gen_banner.py` → 提交
- **卡片排版**：改 `scripts/gen_live_cards.py` → 本地 `LIVE_OFFLINE=1 python3 scripts/gen_live_cards.py` 预览 → 提交
- **项目列表**：直接改 README 的 Projects 区块

## 排查

- 卡片数据不对/不刷新 → 看 Actions 里「🎴 生成实时卡片」运行日志
- 蛇不更新 → 看「🎮 生成贡献贪吃蛇」日志

