# 🚀 部署说明

把「绿黑终端风」主页放到你的 GitHub 主页上，一共 4 步，5 分钟搞定。

## 1️⃣ 备份旧主页（可选但推荐）

先登录 GitHub，进入 `laojiahuo2003/laojiahuo2003` 仓库，把旧 `README.md` 内容复制到本地备份。

## 2️⃣ 把新文件放进去

在你电脑上打开 profile 仓库目录（没有就克隆一份）：

```bash
git clone https://github.com/laojiahuo2003/laojiahuo2003.git
cd laojiahuo2003
```

把本目录的这两个文件复制进去：

- `README.md` → 替换掉仓库里的旧 `README.md`
- `.github/workflows/snake.yml` → 新建的目录（保持这个路径）

## 3️⃣ 推送

```bash
git add .
git commit -m "🎨 升级绿黑终端风主页"
git push
```

推送后刷新你的主页 `github.com/laojiahuo2003`，新的 README 立刻生效（首次可能缓存几秒）。

## 4️⃣ 触发贪吃蛇（只做一次）

1. 打开仓库的 **Actions** 标签页
2. 找到 **「🎮 生成贡献贪吃蛇」** 工作流
3. 点 **Run workflow** 手动跑一次
4. 跑完，主页的贪吃蛇就会变成**你自己的贡献记录**（之前那个蛇是别人的 😅）

之后它会每天自动更新，不用管了。

---

## 🛠 日常维护

### 改终端图片里的文字（whoami / 项目 / 落款）
那三张终端窗口是**图片**（GitHub 不允许给文字直接上色，所以做成图片）。
想改里面的文字（比如 star 数），编辑 `scripts/gen_terminal_svgs.py` 里的内容，然后：

```bash
python3 scripts/gen_terminal_svgs.py   # 重新生成 assets/*.svg
```

把更新后的 `assets/*.svg` 提交推送即可。

> 💡 以后想要 star 数自动更新，可以把图片方案换成 shields.io 徽章，需要的话我可以帮你改。

### 改打字机动画的文案
打字机那行是一个很长的链接，看 `README.md` 顶部。想改循环文字，只需改链接里 `lines=` 后面分号隔开的文字。

### 想让访问计数器换风格
把 `theme=rule30` 改成别的主题（`moebooru`、`3d`、`rule34` 等），参考
[count.getloli.com 主题列表](https://count.getloli.com/)。

---

## 🤔 常见问题

**Q：贪吃蛇图片显示不出来？**
A：检查 Actions 里的工作流是否成功跑过至少一次。还没跑/正在跑的时候，图片是空的。

**Q：为什么没有鼠标跟随特效？**
A：GitHub README 不允许跑 JavaScript，所以鼠标跟随、掉落字符这类特效在 README 里**做不了**。本设计用「打字机动画 + 挥手动图 + 动态统计卡 + 动态贪吃蛇 + 实时访问计数」来补足动感，这是 README 能做到的极限。

**Q：GitHub 统计卡（统计数据）不显示了？**
A：这两个统计卡用的是第三方服务。我用的是正常运行的镜像实例 `github-readme-stats-wheat-six.vercel.app`。如果哪天它失效，把 `README.md` 里两处 `github-readme-stats-wheat-six.vercel.app` 换回官方地址 `github-readme-stats.vercel.app` 即可（官方偶尔会暂停维护，等它恢复就能用）。

**Q：想加「正在听的歌」？**
A：可以接 Spotify 实时播放卡片（novatorem 服务），需要登录授权 Spotify。想要的话来找我，几分钟搞定。
