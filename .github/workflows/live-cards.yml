name: 🎴 生成实时卡片

# 每小时刷新：足迹条 / 每日精选 / 统计 / 作息钟 / 天气（GitHub API + 日报仓库 + open-meteo）
on:
  push:
    branches: [main]
    paths: ['scripts/gen_live_cards.py', 'scripts/gen_visitors.py', 'scripts/gen_clock.py', 'scripts/gen_weather.py', '.github/workflows/live-cards.yml']
  schedule:
    - cron: "23 * * * *"
  workflow_dispatch:

permissions:
  contents: write

# 与贪吃蛇 workflow 共用同一组，串行推送 main，避免互相撞车；后来的排队等，不取消先来的
concurrency:
  group: bot-push-main
  cancel-in-progress: false

jobs:
  cards:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: 检出主页仓库
        uses: actions/checkout@v4

      # 每一步都 continue-on-error：单个数据源挂掉时脚本本身已经会跳过写入以保留旧卡，
      # 这里再加一道兜底，防止极端异常（网络断开、模块 import 失败等）打红整条流水线导致其他卡也不刷新
      - name: 生成实时卡片（足迹 / 精选 / 统计）
        continue-on-error: true
        env:
          GH_TOKEN: ${{ github.token }}
        run: python3 scripts/gen_live_cards.py

      - name: 生成作息钟（Events API）
        continue-on-error: true
        env:
          GH_TOKEN: ${{ github.token }}
        run: python3 scripts/gen_clock.py

      - name: 生成天气卡（open-meteo）
        continue-on-error: true
        run: python3 scripts/gen_weather.py

      # Traffic API 需要 administration 权限，GITHUB_TOKEN 给不了，必须用 PAT（repo secret: GH_PAT）
      - name: 生成访客卡片（Traffic API）
        continue-on-error: true
        env:
          GH_TOKEN: ${{ secrets.GH_PAT || github.token }}
        run: python3 scripts/gen_visitors.py

      - name: 提交并推送
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add assets/*.svg
          # 访客累计数据（GitHub Traffic 只保留 14 天，data/visitors.json 由 gen_visitors.py 持久化）
          [ -d data ] && git add data/ || true
          git commit -m "🎴 刷新实时卡片" || echo "无变化，跳过"
          # 远端可能已被贪吃蛇等其他 workflow 推进，先 rebase 再推
          git pull --rebase
          git push
